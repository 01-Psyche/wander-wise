import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  container: {
    color: "#D1F366",
    marginTop: 20,
    marginLeft: 10,
  },
  header: {
    letterSpacing: -0.5,
    fontWeight: 400,
  },
  typeControl: {
    width: 150,
    borderRadius: 10,
  },
  typeControl: {
    width: 150,
    borderRadius: 10,
  },
  ratingControl: {
    width: 200,
  },
  select: {
    "& .MuiSelect-icon": {
      color: "#454d96", // Change the color of the select dropdown
    },
  },
});

export default useStyles;
